const fs = require('fs');
const args = require('minimist')(process.argv.slice(2));
// const path=require('path');

console.log(args);
 
// const stats=fs.statSync(path.dirname(args));
// console.log("Is Directory ? " + stats.isDirectory());